---
title: Gradle Setup
description: Automatically generate the Gradle code needed to use DAQEM Studios projects as dependencies in your own mods.
---

import DependencySetup from "@site/src/components/wiki/DependencySetup";

# Gradle Setup

If you are developing your own Minecraft mod and want to integrate with **YAML Config**, you will need to add our mods to your development environment.

Use the interactive tool below to automatically generate the exact `gradle.properties` and `build.gradle` code required for your specific Minecraft version and Mod Loader.

<DependencySetup defaultProject="yaml-config" />
