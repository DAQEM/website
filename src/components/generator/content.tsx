import { GeneratorProject } from "./types";

const GeneratorContent = ({
    currentProject,
}: {
    currentProject: GeneratorProject;
}) => {
    return (
        <div id="generator-content" className="bg-card-background-light h-full">
            Enter
            <div></div>
        </div>
    );
};

export default GeneratorContent;
